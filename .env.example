# Copy to .env and fill in your values
# .env is gitignored — never committed

# NetBird setup key (from NetBird dashboard → Setup Keys)
NETBIRD_SETUP_KEY=

# WiFi hotspot password (default: changeme123)
WIFI_PASSWORD=

# Root password for SSH
ROOT_PASSWORD=
